import IncidentWorkspace from "@/components/incident-workspace";

export const metadata = {
  title: "Incidents",
  description: "Acknowledge, contain, and resolve a simulated customer incident with evidence.",
};

export default function IncidentsPage() {
  return <IncidentWorkspace />;
}
